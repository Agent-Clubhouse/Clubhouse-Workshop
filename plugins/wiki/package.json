{
  "name": "wiki",
  "version": "1.2.1",
  "private": true,
  "license": "MIT",
  "description": "Browse and edit markdown wikis stored in external directories",
  "scripts": {
    "build": "esbuild src/main.tsx --bundle --format=esm --platform=browser --external:react --outfile=dist/main.js",
    "watch": "esbuild src/main.tsx --bundle --format=esm --platform=browser --external:react --outfile=dist/main.js --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@clubhouse/plugin-testing": "file:../../sdk/v0.7/plugin-testing",
    "@clubhouse/plugin-types": "file:../../sdk/v0.7/plugin-types",
    "@types/react": "^19.0.0",
    "dompurify": "^3.3.3",
    "esbuild": "^0.24.0",
    "highlight.js": "^11.11.0",
    "jsdom": "^26.0.0",
    "marked": "^15.0.0",
    "react": "^19.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
